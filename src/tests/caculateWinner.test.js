const calculateWinner = require('../util/calculateWinner');

describe("CalculateWinner", () => {
    test('[3] should result in "fizz"', () => {
      expect(calculateWinner([])).toBe('fizz');
    });``
});